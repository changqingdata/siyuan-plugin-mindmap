# ══════════════════════════════════════════════════════════════════════
# 依赖与生成物
# ══════════════════════════════════════════════════════════════════════

node_modules/
src/generated/

# 构建产物（插件包内容，由 scripts/build.mjs 生成）
#
# ⚠️⚠️ 必须带前导 `/` 锚定到**仓库根**。裸写 `i18n/` 或 `index.css`
# 会匹配**任意层级**的同名路径，于是把真源一起吞掉：
#   · 裸 `i18n/`      → 吃掉 `src/i18n/zh_CN.json` / `en_US.json`（i18n 真源！）
#   · 裸 `index.css`  → 吃掉 `src/styles/index.css`（CSS 真源！）
# 后果：仓库里没有这两份真源 → 别人 clone 下来构建不出完整包，
# 而且丢的是**唯一的一份**（`src/generated/` 与根目录产物都是可再生的，
# 真源不是）。实测这三份真源曾被静默排除在仓库之外。
/index.js
/index.js.map
/index.css
/i18n/
/package.zip

# 测试脚手架的全部产物（esbuild 打包、验证页、探针截图）
#
# 这里**没有一个文件是输入**，全是每次跑套件都会重新生成的东西：
#   · `entry.mjs`    —— tests/run.mjs 打包出来的单测入口
#   · `visual.html` / `visual.js` —— tests/visual.mjs 生成的验证页
#   · `*.png`        —— 各支真机探针的截图（`ux:*` / `probe:*` / `diag-*`）
#   · `dump.html`、`probe-png-export.svg` 等一次性证据
#
# 唯一的「读」是 `ux:visual`（tests/kernel/diag-visual.mjs）加载 `visual.html`，
# 而它自己就会提示「先跑 npm run visual」—— 顺序依赖本来就存在，不是靠入库解决的。
#
# ⚠️ 曾经把它们入库，代价是 73 个 PNG 约 46 MB，且**每跑一次套件就有 20 多个被改写**
# → 每次 `git add -A` 都在提交一堆二进制噪声，仓库无谓地涨。
# 截图是给人当场看的，看完即弃，不该进版本库。
#
# ⚠️⚠️ 但**目录本身必须留着**（所以是 `/*` + 反选 `.gitkeep`，不是 `/tests/.build/`）。
# 有 17 个脚本会写截图却**不自建目录**（diag-commands / diag-settings /
# probe-settings-persist / cdp.mjs 等），它们以前能跑纯粹是因为目录被入库文件撑着。
# 直接忽略整个目录 → 全新 clone 里没有 `tests/.build/` → 这些探针一写截图就挂。
# 实测：`diag-canvas-v2` 在目录缺失时仍 99/99（它那支恰好没写截图），
# 所以「跑一支不报错」证明不了安全 —— 要按「谁写了 screenshot()」来排查。
/tests/.build/*
!/tests/.build/.gitkeep

# ══════════════════════════════════════════════════════════════════════
# 本地备份 / 快照
# ══════════════════════════════════════════════════════════════════════

# 重写历史前用 `git clone --mirror` 做的备份、以及工作区快照。
# 体积大（备份 100 MB+）、且随时可由远端重新克隆，绝不该进版本库。
# 注意：名字里带 `.git` 的目录本来就会被 git 拒绝跟踪，这里写出来是为了
# 让人一眼看到「它是备份、不是源码」，顺便防住 `.bundle` 与快照包。
*.bundle
/MindMap-backup-*.git/
/MindMap-snapshot-*.tar.gz

# ══════════════════════════════════════════════════════════════════════
# 缓存
# ══════════════════════════════════════════════════════════════════════

# TypeScript 增量编译信息（tsconfig 目前没开 `incremental`，一旦开了就会冒出这些）
*.tsbuildinfo
.eslintcache
.stylelintcache

# 测试覆盖率报告
/coverage/
/.nyc_output/

# ══════════════════════════════════════════════════════════════════════
# 本地环境变量（别把密钥提交上来）
# ══════════════════════════════════════════════════════════════════════

.env
.env.*
# 例外：示例文件是给人看的，应当入库
!.env.example

# ══════════════════════════════════════════════════════════════════════
# 编辑器 / IDE
# ══════════════════════════════════════════════════════════════════════

.vscode/
.idea/
.fleet/
# VS Code 的「本地历史」，有时会落在工作区里
.history/
*.swp
*.swo
*~

# ══════════════════════════════════════════════════════════════════════
# 操作系统
# ══════════════════════════════════════════════════════════════════════

.DS_Store
Thumbs.db
desktop.ini

# ══════════════════════════════════════════════════════════════════════
# 日志与临时 / 冲突残留
# ══════════════════════════════════════════════════════════════════════

*.log
*.tmp
*.bak
*.orig
*.rej

# 画布与工具交互增强建议

> 第十五轮 · 仅方案，未改动任何代码
> 审视对象：当前工具条（布局切换 / 折叠全部 / 展开全部 / 搜索 / 导出 / 全屏 / 回大纲 / 缩放条 / 小地图 / 面包屑）与画布本体交互
>
> **第十六轮 · 已全部实施** —— 下面的 P0 / P1 / P2 逐条落地，见文末「实施记录」。

---

## 0. 先说结论

当前这版的功能**密度**已经够了 —— 布局、折叠、搜索、导出、下钻、框选、拖拽、多选、键盘全都有。
真正缺的不是「再加几个按钮」，而是三件事：

| 缺口 | 一句话描述 | 为什么现在最痛 |
| --- | --- | --- |
| **A. 缺「编辑」这一档** | 现在只有「看」和「改单个节点」，没有「批量改结构」 | 结构操作一次只能作用于一个节点；多选了也只能看着 |
| **B. 缺「视图记忆」** | 布局/主题/缩放不跟文档走，换一篇就得重调 | 每次打开都在重复劳动，久了就不想用了 |
| **C. 缺「反馈」** | 操作成功/失败/进行中，全靠 `showMessage` 一闪而过 | 结构操作是异步写内核的，用户不知道到底成没成 |

下面按 **P0（先做）→ P1（值得做）→ P2（有余力再说）** 分档，每条都给「现状 → 方案 → 收益 → 代价」。
最后附一份「**不建议做**」的清单，免得越加越乱。

---

## P0 · 三条骨架级的

### P0-1 多选之后的批量操作条

**现状**
框选（拖拽空白）和 Ctrl 点选已经能选中多个节点了，但选完没有任何后续动作 ——
多选在功能上是个死胡同，用户框完只能按 Delete 删掉，或者一个个拖。

**方案**
选中 ≥2 个节点时，在**工具条下方浮出一条「批量操作条」**（不是弹窗，避免遮挡画布）：

```
┌──────────────────────────────────────────────────────────┐
│  已选 5 个  ·  [ 升级 ]  [ 降级 ]  [ 折叠 ]  [ 展开 ]  ·  [ 导出这些 ]  [ 删除 ]  [ ✕ ] │
└──────────────────────────────────────────────────────────┘
```

- **升级 / 降级**：批量改层级。这是最高频的诉求（「这几条都该是上一级的子节点」），
  现在只能一条条按 `Alt+→`。
- **折叠 / 展开**：只作用于「选中的这些节点」，与工具条上的「折叠全部」区分开。
- **导出这些**：只导出选中子树，直接复用已有的导出管线。
- **删除**：带撤销（撤销栈已经有了）。

**收益**：把「多选」从一个半成品变成真正能提效的功能。
**代价**：中等。`actions.ts` 里的每个结构操作要支持「批量」入口；
内核没有批量 API，批量 = 循环调用，所以必须**先算好全部计划再逐条执行**，
中途失败要能回滚（`history.ts` 的撤销记录已经能承载）。

---

### P0-2 结构操作的结果反馈

**现状**
`applyAction()` 里失败只有一句 `showMessage("操作未生效，请重试")`，
成功则**完全没有反馈**。而这条链路是异步的：写内核 → 内核回推 DOM → 扫描重挂视图，
中间隔了 140ms 扫描防抖 + 内核往返，用户点完「+」之后有 300~500ms 是**没有任何变化**的。

于是用户会：① 以为没点上，再点一次（结果插了两个）；② 以为插件坏了。

**方案**
两层反馈，都不打断操作：

1. **乐观占位**：点「+」的瞬间，先在导图上插一个半透明的「新节点」占位框（虚线边框 + 呼吸动画），
   位置按预期布局算好。等内核回推、视图重挂之后，占位框被真节点替换。
   这一条和本轮折叠同步用的「覆盖表」是同一个思路：**先给反馈，再等真相**。
2. **失败要显眼**：失败时不是弹一句提示，而是在**原节点上**打一个红色描边 + 抖动 200ms，
   并把失败原因放进 tooltip（「该块已被删除」「父列表已变更」）。

**收益**：消除「点了没反应」这个最容易劝退的体验断层。
**代价**：中等偏低。占位框可以复用 `createNodeEl`，只是加个 `mm-placeholder` 类。

---

### P0-3 视图状态跟文档走

**现状**
`layout / theme / edge / 缩放 / 平移` 全部是**全局设置**，不跟文档走。
在 A 文档里调成「思维导图 + 深色主题」，切到 B 文档也是这套；
反过来，B 文档本来调好的「树状图」，回到 A 又被改了。

**方案**
把「视图偏好」拆成两级：

- **全局默认**（设置面板里那套）—— 新文档第一次打开时用它。
- **文档级覆盖** —— 用户在这个文档里改过布局/主题之后，写进**块属性**
  （`custom-mindmap-view`，值形如 `logic|deep|1.25`），下次打开这个列表就恢复它。

关键设计：**只记「用户显式改过的项」**，没改过的继续跟随全局默认 ——
否则用户改了全局主题，所有文档都不跟着变，反而更别扭。

另外把**缩放与平移**也纳入：现在重新打开文档一定回到「适应画布」，
用户精心放大到某个分支看细节，切走再回来就没了。建议记 `scale`，
但**不记平移**（平移跟画布尺寸强相关，记了很容易错位）。

**收益**：从「每次都要重调」变成「打开就是上次的样子」。
**代价**：低。已经有 `custom-mindmap` 属性的读写管线，加一个属性即可。
注意要跟本轮「折叠状态完全跟随大纲」的原则一致 ——
**布局/主题是用户偏好，可以记；折叠是内容状态，必须交给大纲**，两者不要混。

---

## P1 · 让画布「会说话」

### P1-1 悬停预览：把隐藏的子树「透出来」

折叠一个节点之后，用户其实想知道「里面是什么」，但现在只有珠子上的数字。
**方案**：悬停折叠节点 600ms，在它旁边浮出一张小卡片，列出前 5 个子节点文字 + 「还有 N 个」。

顺带解决另一个问题：**拖拽时悬停自动展开**（已有 `HOVER_EXPAND_DELAY`）现在没有任何提示，
用户不知道「停一下会展开」。加一个环形进度指示就够了。

**代价**：低。纯 UI。

---

### P1-2 连线语义化

现在所有连线长得一样。但大纲里其实藏着信息：

- **任务列表**：`☑ 已完成` / `☐ 未完成` —— 已完成的子树连线可以用虚线。
- **有序列表**：层级编号已经在节点上显示了，连线粗细可以随层级递减（现在已经做了渐细）。
- **新插入的节点**：给一条 1.5s 的高亮描边，让用户知道「加在哪了」。

**代价**：低。`buildConnectors` 加一个 `style` 参数即可。

---

### P1-3 小地图增强：从「看的」变成「用的」

现在小地图只显示视口框，不能交互。
**方案**：

- 点击小地图 → 视图直接跳过去（现在只能拖视口框）。
- 小地图上**标出搜索命中点**（小黄点）和**选中节点**（小蓝点）。
- 节点数少（比如 < 30）时**自动隐藏小地图** —— 现在小图上也挂个小地图，纯占地方。

**代价**：低。

---

### P1-4 缩放条升级成「缩放菜单」

现在缩放条是 `[−] [100%] [+] [适应]`，点了 `100%` 是「回到 1.0」——
但用户真正想要的是「**缩放到能看清这个分支**」。

**方案**：把 `100%` 那个标签做成下拉：

```
[−] [ 100% ▾ ] [+] [适应]
        ├─ 100%
        ├─ 适应画布
        ├─ 适应选中节点        ← 新增
        ├─ 只看当前分支        ← 等价于下钻，但更直观
        └─ 记住这个缩放        ← 写进 P0-3 的文档级偏好
```

**代价**：低。

---

## P2 · 锦上添花

| 项 | 说明 | 代价 |
| --- | --- | --- |
| **P2-1 大纲双向高亮** | 光标在大纲里移动时，导图对应节点跟着亮；反之亦然 | 中（要挂 Protyle 的事件） |
| **P2-2 导出增强** | 导出时可选「只导选中」「导出为 Markdown 大纲」「复制为图片到剪贴板」 | 低 |
| **P2-3 演示模式** | 全屏之上再加一档：逐层展开 + 方向键推进，适合讲结构 | 中 |
| **P2-4 自定义配色** | 现在 6 套主题是写死的。允许用户改一级分支的色板 | 低 |
| **P2-5 布局动画微调** | FLIP 已经有了，可以加「折叠时子节点向父节点收拢」的动画 | 低 |

---

## 不建议做（明确劝退）

这些看起来诱人，但会破坏「大纲是唯一真相源」这个根基：

1. **不要在导图里直接新建「游离节点」** —— 所有节点必须是真实块，否则一保存就丢。
2. **不要让导图自己维护一份折叠状态** —— 本轮刚把这条拆掉，回到原生 `fold` 之后
   「大纲什么状态、导图就是什么状态」才是结构上成立的。
3. **不要加「自动整理布局」按钮** —— 导图的位置完全由布局算法推导，没有手工位置，
   所以这个按钮无处施力；真要「整理」，用户需要的是「切布局」。
4. **不要在小地图上做节点级操作** —— 小地图的作用是「导航」，塞进编辑操作只会误触。
5. **不要把设置项继续往设置面板里堆** —— 现在设置面板已经有 20+ 项。
   新东西优先做成**画布内的就地操作**（悬停操作条、右键菜单），
   用户不用离开画布就能改。

---

## 建议的落地顺序

```
第一步（一次迭代内可完成，收益最大）
  P0-2 结构操作反馈   ← 纯前端，独立，见效最快
  P0-3 视图状态跟文档走 ← 复用已有属性管线

第二步（需要动 actions.ts）
  P0-1 批量操作条     ← 依赖结构操作支持批量

第三步（纯打磨）
  P1-1 / P1-2 / P1-3 / P1-4
```

---

## 附：当前交互清单（供对照，避免重复设计）

**工具条**：逻辑结构图 / 思维导图 / 树状图 · 折叠全部 / 展开全部 · 搜索 / 导出 / 全屏 / 回大纲
**缩放条**：− / 100% / + / 适应
**画布**：拖拽平移 · Ctrl 滚轮缩放 · 滚轮平移（可配）· 框选 · Ctrl 点选 · 节点拖拽改结构
**节点悬停条**：+ 加子节点 · ⊙ 聚焦 · ▾ 菜单
**右键菜单**：加子节点 / 插入同级 / 聚焦 / 复制子树 / 快速复制 / 粘贴 / 改名 / 定位正文 / 上移 / 下移 / 升降级 / 折叠 / 删除
**键盘**：↑↓←→ 导航 · 空格 折叠 · F2 改名 · Tab/Shift+Tab · Enter 同级 · Delete · Alt+←/→ · Ctrl+↑↓ · Ctrl+C/V/X/D · Ctrl+=/-/0/1 · Ctrl+F · F 全屏 · Esc
**小地图**：显示视口框（可拖）
**面包屑**：下钻路径（可点返回）

---

# 实施记录（第十六轮）

上面 P0 / P1 / P2 逐条落地。这份记录只写**「和原方案不一样的地方」与「实施中挖出的问题」**，
方案本身的描述不重复。

## 落地清单

| 条目 | 落地位置 | 与原方案的差异 |
| --- | --- | --- |
| P0-1 批量操作条 | `renderer.ts` `buildBatchBar` / `batchFold` / `runBatchAction`，`actions.ts` `indentNodesInto` / `outdentNodes` / `deleteNodes`，`tree.ts` `topLevelOf` | 条子改成**浮层**（原方案是流内元素）—— 见下面「实施中挖出的问题」第 5 条 |
| P0-2 乐观占位 | `renderer.ts` `showGhost` / `dropGhost` / `flashNode` / `runAction`，`scanner.ts` `applyAction` 改为返回 `Promise<MMActionResult>` | 无 |
| P0-3 视图跟文档走 | `core/prefs.ts`（编解码），`renderer.ts` `applyViewPrefs` / `markPref` / `forgetPref`，块属性 `custom-mindmap-view` | 编码用 `key=value;` 而不是位置编码 —— 加字段不用动老数据解析 |
| P1-1 悬停预览 | `renderer.ts` `armPreview` / `showPreview` / `markHoverExpand` | 无 |
| P1-2 连线语义化 | `core/edge.ts` `buildConnectors` 支持 `kidDash`；`renderer.ts` `drawEdges` | 顺带修掉 `horizontal` 分组丢失原始下标 |
| P1-3 小地图增强 | `renderer.ts` `refreshMinimap` / `minimapMarkSvg` / `updateMinimapMarks` | 阈值 50 → **30** 个节点；标记单独成层 |
| P1-4 缩放菜单 | `renderer.ts` `openZoomMenu` / `fitToNodes` / `toggleRememberScale` | 无 |
| P2-1 双向高亮 | `renderer.ts` `bindOutlineCursor` / `markOutlineCursor` | 无 |
| P2-2 导出增强 | `core/exporter.ts` `renderPngBlob` / `exportOutline` / `ExportCrop`；`renderer.ts` `prepareExport(only)` | 无 |
| P2-3 演示模式 | `renderer.ts` `togglePresent` / `presentGo` / `presentFold` | 无 |
| P2-4 自定义配色 | `types.ts` `customPalette`，设置面板 | 无 |
| P2-5 收拢动画 | `renderer.ts` `detachVanishing` / `runCollapse` | 无 |

「不建议做」的五条一条没做。

## 实施中挖出的 5 个真问题

1. **选择对象在重渲染后失联**（最关键）。`render()` 整棵重建 `MMNode`，而 `selected` /
   `extraSel` 握着旧对象 → `selectedInDocOrder()` 用对象同一性在新树里找，恒返回空数组
   → 批量操作第二次点击静默失效。修法：`remapSelection()` 按块 ID 重绑。
2. **`syncToolbar()` 从不在挂载时调用** → 分段控件三个按钮永远不亮。
3. **`refreshSelection()` 不刷小地图标记** → 选择变化不触发重渲染，标记层永远停在上一帧。
4. **Esc 关不掉插件自己的菜单**。键盘挂在 `document` 捕获阶段，抢在思源 Menu 之前拿到 Esc。
   顺带发现 `Menu.isOpen` 在 3.8.4 上不可靠（`open()` 之后仍是 `false`）。
5. **批量条占布局空间 → 推动画布**。`Ctrl+单击` 在导图里是多选，条子一出现把画布下推约 40px，
   于是 `Ctrl+双击下钻` 的第二下打在空白上、整条链路失效。改成浮层后从根上消失。

## 验收

- `npm run ux:canvas`（`tests/kernel/diag-canvas-v2.mjs`，新建）**56/56**
- `npm run check`：typecheck + 250 断言 + 96 冒烟
- `npm run ux:all`：九支真机回归
- 排障探针：`tests/kernel/introspect-v2.mjs`、`probe-unfold-api.mjs`、`probe-batch-fold.mjs`、`probe-menu.mjs`

### 验收补全：两条「有实现、没断言」的条目

首轮 44 项里，有两条交付是**只有实现、没有真机覆盖**的 —— 都不是功能坏了，
而是它们没有可观测的静态痕迹，写断言时被跳过了。后面补上了：

| 条目 | 为什么难测 | 怎么测的 |
| --- | --- | --- |
| P1-1 后半 · 拖拽悬停的**环形进度** | `.mm-toggle--loading` 只活 `HOVER_EXPAND_DELAY`（420ms）那一小段，且只在拖拽过程中存在，查最终 DOM 永远是 0 | `WATCH(".mm-toggle--loading")` 抓 attributes 变更；按下 → 横移 14px 激活拖拽（过 `DRAG_THRESHOLD`）→ 落到折起分支中心带 → 240ms 内断言环在场 → `waitFor` 可见节点数上涨（自动展开）→ 移开空白处松手 |
| P2-4 **自定义一级分支配色** | 只能从设置面板改，走 UI 要开 Dialog、找 DOM、点确认，脆 | 直接照抄设置确认后的后半段：改内存里的 `config.customPalette` → `scanner.refreshAll()`，再读一级分支的 `--c-solid` |

新增 12 项断言（`ux:canvas` 44 → **56**），覆盖：环出现 / 环在场 / 自动展开 / 松手撤环 /
没误触发移动；色板顺序生效 / 短色板循环取用 / 非法输入退回主题 / 清空后回默认。

**顺带钉住的两件事**（原方案没写、但属于同一行为的边界）：

- 色板短于分支数时**循环取用**（`palette[branch % palette.length]`），不该掉成透明；
- 色板全是非法值时**整体忽略**退回主题色板，而不是渲染成一片透明 —— 这条在
  `paletteOf()` 的注释里写了「免得把整张图渲染成一片透明」，现在有断言守着。

**方法论补充**：给「瞬态 / 无 UI 入口」的功能写断言，优先用 `WATCH`（MutationObserver）
而不是查终态；能给内存态就直接给内存态，别为了「像用户操作」去点三层 UI ——
后者测的是设置面板，不是配色。

## 收尾：把探针的「垃圾回收」修好

补断言时顺手查了一次工作空间，发现笔记本里积了 **143 个**「临时-xxx」文档 ——
全是历轮真机探针建了没删的。根因有三层，一层比一层隐蔽：

**① `/api/filetree/removeDoc` 不认 `{id}`。** 在思源 3.8.4 上它会返回：

```json
{"code":-1,"msg":"Field [notebook] is required"}
```

正确姿势是两步 —— 先 `getPathByID` 拿 `{notebook, path}`（**字段叫 `notebook`，不是 `box`**），
再 `removeDoc {notebook, path}`。而探针的清理全写成 `.catch(() => {})`，
错误被静默吞掉，于是每跑一轮就漏一批，谁也不知道。

**② `/api/block/deleteBlock {id}` 删不掉文档。** 它只清块，`.sy` 文件留在磁盘上、
文档也还挂在文件树里。五支探针（edit-flow / reopen / feedback / fold-sync / zoom-measure）
都用了它 —— 每跑一轮 ux:all 就固定漏 5 个。

**③ `/api/query/sql` 默认只回 64 行。** 写 `LIMIT 500` 也不生效。
第一次扫只看到 64 个就以为「一共 64 个」，实际是 143 个 —— 79 个静默消失。

修法：

- 抽出 `tests/kernel/_doc-cleanup.mjs`，导出 `removeDoc(api, id)` / `removeDocs(api, ids)`，
  把「两步 + 字段名 + 失败要出声」封在一处；十一支探针统一改走它。
- 新增 `scripts/clean-tmp-docs.mjs`（`npm run clean:tmp` / `clean:tmp!`），
  按严格签名 `临时-<探针名>[-legacy|-native]-<时间戳>` 扫掉历史遗留，
  显式翻页绕开 64 行上限，默认 dry-run、加 `--yes` 才真删。
- `diag-user-doc.mjs` 的默认目标原本写死成一个**探针漏下来的临时文档 ID** ——
  垃圾一清它就再也跑不起来。改成没给参数就现造对照文档，收尾自删。

清理结果：**143 个全删**；再跑一遍 `ux:all` 后复查，**新增 0 个**。

**两个反直觉的索引行为**（都踩过，都写进注释了）：

- 建完文档**立刻**查 SQL 查不到 —— 「查不到」不等于「没建出来」，别据此判创建失败；
- 删完文档**立刻**查也还有 —— 会看到「还剩 22 个」，过 20 秒再看变 0。
  判断到底删没删，看磁盘（`data/<notebook>/*.sy`）比看 SQL 靠谱。

## 最终验收

- `npm run ux:canvas` **56/56**
- `npm run ux:all` 九支串跑 **exit=0**（19/19、11/11、26/0、56/56 …），
  跑完工作空间**零残留**
- `npm run check`：typecheck + 250 断言 + 96 冒烟 + 160.2kb / 104.0KB zip
- `npm run deploy` 已同步

**方法论**：第一次跑验收时 11 项失败，其中只有 3 项是真 bug，其余 8 项是探针判据写错。
先内省（把 DOM 真相 dump 出来）再改代码，否则会照着假失败越改越坏。
